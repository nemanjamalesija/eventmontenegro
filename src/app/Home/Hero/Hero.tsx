import { FC } from 'react';
import styles from './hero.module.css';
import clsx from 'clsx';

const Hero: FC = () => {
  return <div className={styles.section__hero}>ssss</div>;
};

export default Hero;
